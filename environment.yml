name: adcircpy
channels:
  - defaults
dependencies:
  - bs4
  - matplotlib
  - netcdf4
  - numpy
  - pandas
  - pip
  - psutil
  - pyproj>=2.6
  - requests
  - scipy
  - seaborn
  - shapely
  - pip:
      - coverage
      - coverage-badge
      - eventlet
      - haversine
      - nose
      - ordered-set
      - rednose
      - utm
      - wget
